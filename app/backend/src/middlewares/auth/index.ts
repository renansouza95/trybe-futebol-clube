import authenticateToken from './tokenAuth';
import authenticateLogin from './loginAuth';
import { authenticateMatches, authenticateTeams } from './matchAuth';

export {
  authenticateToken,
  authenticateLogin,
  authenticateMatches,
  authenticateTeams,
};
